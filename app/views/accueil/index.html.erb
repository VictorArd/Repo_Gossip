

<div class="container text-center">

  <h1>Bienvenue sur notre site Gossip!</h1>
  <p>Tous nos Gossip sont dans la liste ci dessous classé par auteur et par titre</p>

	<div class="card-columns">
			<% @potins.each do |potin| %>

	      <div class="card" style="width: 18rem;">
	      	<div class="card-body">
	    			<h5 class="card-title"><%= potin.user.f_name + ' ' + potin.user.l_name + " : " %></h5>
	    			<p class="card-text"><%= link_to potin.title, potin_path(potin.id), :class => "link"%></p>
	    		</div>
  			</div>

  		<% end %>
  </div>

	<div>
	<p class="text-center"><%= link_to "Créer un potin !", new_potin_path, :class => "btn btn-success"%></p>
	</div>

</div>

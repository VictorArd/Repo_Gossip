
<div class='conteneur'>
  <%= form_tag url_for(action: 'create'), method: "post" do %>

    <%= label_tag 'Titre' %>
    <%= text_field_tag 'title', @potin.title, class: 'form-control', placeholder: "Titre" %>
    <br>

    <%= label_tag 'Contenu' %>
    <%= text_area_tag 'content', @potin.content, class: 'form-control', placeholder: "Contenu" %>
    <br>

    <%= label_tag 'Auteur' %>
    <%=select_tag 'author', options_for_select(@users.map{ |user| [user.f_name, user.id] }), class: 'form-control', placeholder: "Autheur" %>
    <br>

    <p class="commit text-center"><%= submit_tag "Créer le potin", class: "btn btn-primary"%></p>

  <% end %>
